
#
# below is a link to a file sourced here:
# https://github.com/dsifford/yarn-completion
# all copyrights are listed in that file
# follow directions from that site to install yarn completion.ß
#

[[ -r "/usr/local/etc/profile.d/bash_completion.sh" ]] && . "/usr/local/etc/profile.d/bash_completion.sh"
